let express,mongoose,contactRoutes;_663‍.w("express",[["default",["express"],function(v){express=v}]]);_663‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_663‍.w("./routes/contact.routes",[["contactRoutes",["contactRoutes"],function(v){contactRoutes=v}]]);



const app = express();

const PORT = 3000;


app.use(express.json());
app.use(express.urlencoded({extended: true}));

mongoose.Promise = global.Promise;

// Connect MongoDB at default port 27017.
mongoose
  .connect("mongodb://localhost:27017/contact-numbers", {
    useNewUrlParser: true,
    useCreateIndex: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    app.listen(() => {
        _663‍.g.console.log('App running on port', PORT);
    })
    console.log("MongoDB Connection Succeeded.");
  })
  .catch((err) => {
    _663‍.g.console.log("Error in DB connection: " + err);
  });

app.use('/contacts', contactRoutes)